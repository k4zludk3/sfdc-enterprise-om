public inherited sharing class StandardPriceStrategy implements IPricingStrategy{
    public Decimal calculatePrice(Decimal subTotal) {
        // Standard customers pay full price
        return subtotal;
    }
}